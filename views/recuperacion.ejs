<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title> Iniciar Sesión </title>

   <!-- CSS only -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
   <style>
      body{
         width: 100%;
         background: rgb(45,45,135);
         background: linear-gradient(90deg, rgba(45,45,135,0.9920343137254902) 25%, rgba(148,235,205,1) 100%);
      }
      .diseñadores{
         display: flex;
         align-items: center;
      }
      .diseñadores p {
         margin: 0 0.5rem;
      }
   </style>
</head>

<body>
  <h2 class="text-center mt-5"><%= title %></h2>
   <div class="container d-flex flex-row justify-content-center">
      <div class="row">
         <div class="col">
            <form target="/login" method="POST" class="card card-body m-3" style="width: 50rem">
               <div class="form-group p-2">
                  <label for="email">Email</label>
                  <input id="email" type="email" name="Email" class="form-control" placeholder="correo@correo.com" required/>
               </div>
               <div class="form-group p-2">
                  <label for="password">Contraseña Actual</label>
                  <input id="passwordOld" type="password" name="PasswordOld" class="form-control" placeholder="Password" required/>
               </div>
               <div class="form-group p-2">
                  <label for="password">Contraseña Nueva</label>
                  <input id="password" type="password" onchange="obtenerValores()" name="Password" class="form-control" placeholder="Password" required/>
               </div>
               <div class="form-group" hidden  id="error">
                  <p class="text-danger p-2 m-1">Contraseñas no son iguales, intentelo de nuevo</p>
               </div>
               <div class="form-group p-2">
                  <label for="password">Repetir Contraseña Nueva</label>
                  <input id="passwordRepeat" type="password" onchange="obtenerValores()" name="PasswordRepeat" class="form-control" placeholder="Password" required/>
               </div>
               <div class="form-group" hidden id="error">
                  <p class="text-danger p-2 m-1">Contraseñas no son iguales, intentelo de nuevo</p>
               </div>
               <input id="rol" type="text" value="user" hidden name="rol" class="form-control" required/>
               <input type="submit" id="submit" disabled value="Iniciar Sesion" class="btn btn-dark m-2">
            </form>
         </div>
      </div>
   </div>
</body>

<script>
   function obtenerValores() {
      let passw = document.getElementById('password').value
      let passwRep = document.getElementById('passwordRepeat').value
      let submit = document.getElementById('submit')
      let err = document.querySelectorAll('#error')
      if (passw == passwRep) {
         err.forEach(error => {
            error.setAttribute("hidden", false)
         });
         submit.removeAttribute('disabled')
      } else {
         err.forEach(error => {
            error.removeAttribute('hidden')
         });
         submit.setAttribute('disabled', true)
      }
   }
</script>
<%- include("templates/footer") %>
